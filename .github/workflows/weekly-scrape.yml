name: weekly-scrape
on:
  schedule:
    - cron: '0 2 * * 1' # every Monday 02:00 UTC (05:00 local) — 02:00 UTC (05:00) 
  workflow_dispatch: {}

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
      - run: npm ci
      - run: node scripts/scrape.js
        env:
          TARGET_URL: 'https://wiki.warframe.com/w/Baro_Ki%27Teer/Trades'
      - name: Commit data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/warframe_data.json
          git commit -m "Weekly scrape: $(date -u +'%Y-%m-%dT%H:%M:%SZ')" || echo "No changes"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
